# 面接想定問答 復習ノート

演習中に自分の言葉でまとめた回答を蓄積していく。曖昧な点は都度アップデートする。

---

## Week1 Day5(C言語の落とし穴・volatile・const・malloc)

### Q. volatileとは?

volatileとはCのコンパイラによる最適化でアドレスの値を書き換えていないと判断してしまい、最初の値だけを読み以降はメモリにアクセスしなくなってしまうので、volatileをつけることによって毎回メモリアクセスをするように指示できるものです。
例えばハードウェアのレジスタや割り込みハンドラとメインループで共有する変数において使われます。

*(補足: volatileは「最適化の抑止」であって「排他制御(atomic)」ではない。複数スレッド/割り込みからの同時読み書きの安全性は別途atomicや割り込み禁止区間で守る必要がある — Week3で扱う内容)*

### Q. constをポインタに付ける2つの位置の違いは?

constの位置によって決まります。
- constが先頭にある場合(`const int *p`)は、直後の型(int)にかかる → `*p`(指す先の値)が変更不可。英語では **pointer to const**。
- constが`*`の後にある場合(`int *const p2`)は、ポインタ自体にかかる → `p2`自体(指す先を変える操作)が変更不可。英語では **const pointer**。

読み方のコツ: constは直前にあるものにかかる。ただしconstが先頭に来ているときだけ、直後のものにかかる。

### Q. mallocを組み込みで避ける理由は?

組み込み機器は再起動なしで長く稼働させることが珍しくなく、その中でメモリの確保と解放を繰り返すとメモリが虫食い状態(断片化)になり、メモリの空きがあるのに確保に失敗するようなことがあります。
またmallocを呼ぶたびに空いている領域を探す処理が挟まるので、断片的な場合は時間が多くかかったりなど、かかる時間が一定ではありません(非決定性)。それによってリアルタイム制御で時間の制約がある処理に対して最悪の実行時間を保証できず、リアルタイム処理の締め切りを守れない可能性が出てきてしまうからです。

キーワード: **断片化**・**非決定性**

---

## Week2 Day5(RAII・スマートポインタ・組み込みC++)

### Q. RAIIとは?

RAIIはコンストラクタで初期化(リソースの確保)などを行い、デストラクタで解放をすることで自動的に処理を行うことから、閉じ忘れなどのミスが起こらないようにする設計のこと。

*(具体例: `FileHandle`クラスで、コンストラクタで`fopen`、デストラクタで`fclose`。正常終了・途中return・例外による離脱、どのケースでもデストラクタは必ず自動的に呼ばれることを実験で確認した)*

### Q. unique_ptrとshared_ptrの使い分けは?

所有者が1人のものはunique_ptr、複数の所有者が必要なものはshared_ptrを使う。
shared_ptrは循環参照でリークするなど取り扱いのコストが比較して高くなるので、基本はunique_ptrを使い、本当に必要なときだけshared_ptrを使うとよい。

*(実験: shared_ptr同士が相互参照する循環参照を実際に作り、valgrind/AddressSanitizerでリークを確認。weak_ptrに片方を変えることで解消した)*

### Q. 組み込みで例外を避ける理由は?

例外処理の記述によってコードサイズの増加や、実行時間が読めないことなどの非決定性が起きてしまうので、現場では例外(RTTI含む)を無効化する(`-fno-exceptions`)ことが多い。

キーワード: **コードサイズ増加**・**非決定性**(mallocを避ける理由と同じ構造)

---

## 使い方メモ

- 新しい問答は、演習で実際に手を動かした直後にこのファイルへ追記する
- 音読して3分以内に収まるか、時々チェックする
